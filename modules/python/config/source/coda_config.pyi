"""
Type stubs for coda_config module.

This module serves as a configuration and dependency marker for the
CODA-OSS Python bindings. It does not export any classes or functions
but ensures proper module initialization order.
"""

# Empty module - used for dependency tracking
